apiVersion: apps/v1
kind: Deployment
metadata:
  name: auto-discovery-specs-api
  namespace: auto-discovery
  labels:
    app: auto-discovery-specs-api
    tier: backend
    service: specs
    version: v1.0.0
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auto-discovery-specs-api
  template:
    metadata:
      labels:
        app: auto-discovery-specs-api
        tier: backend
        service: specs
        version: v1.0.0
    spec:
      nodeSelector:
        kubernetes.io/hostname: thor
      containers:
      - name: specs-api
        image: localhost/auto-discovery-specs-api:v1.0.0
        imagePullPolicy: Never
        ports:
        - containerPort: 3000
          name: http
        env:
        - name: ELASTICSEARCH_URL
          value: "http://elasticsearch.data.svc.cluster.local:9200"
        - name: ELASTICSEARCH_INDEX
          value: "autos-unified"
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        - name: SERVICE_NAME
          value: "specs-api"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
